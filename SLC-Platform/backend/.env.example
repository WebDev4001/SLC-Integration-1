# PostgreSQL Database Configuration
# Option 1: Using individual variables (recommended for more control)
POSTGRES_DB=slc_db
POSTGRES_USER=slc_user
POSTGRES_PASSWORD=your_secure_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Option 2: Using DATABASE_URL (if your settings.py is configured to parse it, e.g., with dj-database-url)
# DATABASE_URL=postgres://slc_user:your_secure_password@localhost:5432/slc_db

# Django Secret Key
# IMPORTANT: Generate a strong, unique secret key for your production environment.
# You can generate one using: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY='your_django_secret_key_here_replace_this_in_production'

# Debug Mode
# IMPORTANT: Set to False in production for security.
DEBUG=True

# Allowed Hosts (comma-separated list of domains/IPs that can serve this site)
# Example for development: ALLOWED_HOSTS=localhost,127.0.0.1
# Example for production: ALLOWED_HOSTS=.yourdomain.com,yourdomain.com
ALLOWED_HOSTS=localhost,127.0.0.1

# CORS Configuration
# Comma-separated list of origins allowed to make cross-site requests.
# Example for local Angular dev (port 4200): CORS_ALLOWED_ORIGINS=http://localhost:4200
# Example for multiple origins: CORS_ALLOWED_ORIGINS=http://localhost:4200,https://yourfrontend.com
# For allowing all origins (less secure, useful for public APIs or initial dev): CORS_ALLOW_ALL_ORIGINS=True
CORS_ALLOWED_ORIGINS=http://localhost:4200

# Google Client ID (for Google Sign-In token validation on the backend)
GOOGLE_CLIENT_ID='your_google_oauth_client_id.apps.googleusercontent.com'

# JWT Settings (defaults from djangorestframework-simplejwt are often fine)
# ACCESS_TOKEN_LIFETIME_MINUTES=30
# REFRESH_TOKEN_LIFETIME_DAYS=7

# Email Backend (for password reset, email verification - if implemented)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@example.com
# EMAIL_HOST_PASSWORD=your-email-password
# DEFAULT_FROM_EMAIL=webmaster@localhost
